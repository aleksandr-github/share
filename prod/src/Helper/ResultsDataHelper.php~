<?php

namespace App\Helper;

class ResultsDataHelper
{
    public static function isResultBetter($result, $results)
    {
        if ($result['position'] == $results['position']) {
            return false;
        } elseif ($result['position'] < $results['position']) {
            return true;
        }

        return false;
    }
}